use_default_settings: true

general:
  debug: false
  instance_name: "Agent RAG SearXNG"

search:
  safe_search: 0
  autocomplete: ""
  default_lang: "auto"
  formats:
    - html
    - json

server:
  bind_address: "0.0.0.0"
  port: 8080
  limiter: false
  image_proxy: false
  method: "GET"
  secret_key: "agent-rag-local-only-change-me"

valkey:
  url: false
