# graph-rag demo

Active project files live here.

Layout:

- `graph_rag.py`: CLI entrypoint
- `graph_rag_app/`: application code
- `tests/`: automated tests
- `agent/`: local retrieval index artifacts
- `runtime/`: generated checkpoints and logs

Use this directory as the working directory when running the demo.
