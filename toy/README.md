# toy

`main/` is the active Graph RAG demo project.

`archive/` stores older prototypes, one-off scripts, and runtime leftovers that are
kept only for reference.

Run and develop from `toy/main`, not from the `toy/` root.
